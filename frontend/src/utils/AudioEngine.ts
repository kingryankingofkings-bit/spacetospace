export const playBGM = (zone: string) => {
    console.log("Playing BGM for zone:", zone);
};

export const playHitSound = (position: [number, number, number]) => {
    console.log("Playing hit sound at:", position);
};
