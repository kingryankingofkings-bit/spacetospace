# Roadmap Packs 1-12 Production Brief

## Pack 1: Modular Environment Kit

Primary ZIP: `modular_environment_shop_quest_glb_pack_v1.zip`
Detected GLBs: `57`
Detected WAVs: `0`

Production focus: convert environment modules into grid-snapped prefabs with collision, snapping sockets, material variants, and navigation rules.

Required Antigravity 2.0 actions:

1. Import source assets without renaming internal mesh nodes unless a deterministic mapping table is written.
2. Generate or assign colliders from `colliders.json` when available.
3. Create engine prefabs with a stable prefab ID equal to the source filename stem.
4. Add category-specific scripts/components according to the prefab schema.
5. Generate LOD/proxy variants for repeated world assets.
6. Validate prefab spawn, render, collision, and unload behavior.

## Pack 2: Biome-Specific Prop Packs

Primary ZIP: `biome_specific_props_glb_pack_v1.zip`
Detected GLBs: `72`
Detected WAVs: `0`

Production focus: tag by biome, make instanced prop prefabs, assign simple colliders, and create density/placement rules.

Required Antigravity 2.0 actions:

1. Import source assets without renaming internal mesh nodes unless a deterministic mapping table is written.
2. Generate or assign colliders from `colliders.json` when available.
3. Create engine prefabs with a stable prefab ID equal to the source filename stem.
4. Add category-specific scripts/components according to the prefab schema.
5. Generate LOD/proxy variants for repeated world assets.
6. Validate prefab spawn, render, collision, and unload behavior.

## Pack 3: Shop and Town Interior Assets

Primary ZIP: `modular_environment_shop_quest_glb_pack_v1.zip`
Detected GLBs: `57`
Detected WAVs: `0`

Production focus: convert shop/interior meshes into interactive shop, inn, blacksmith, dye, and display prefabs.

Required Antigravity 2.0 actions:

1. Import source assets without renaming internal mesh nodes unless a deterministic mapping table is written.
2. Generate or assign colliders from `colliders.json` when available.
3. Create engine prefabs with a stable prefab ID equal to the source filename stem.
4. Add category-specific scripts/components according to the prefab schema.
5. Generate LOD/proxy variants for repeated world assets.
6. Validate prefab spawn, render, collision, and unload behavior.

## Pack 4: Item Pickup and Equipment Display

Primary ZIP: `item_pickup_equipment_display_glb_pack_v1.zip`
Detected GLBs: `75`
Detected WAVs: `0`

Production focus: map item database entries to archetype prefabs, rarity glows, loot beams, pickup triggers, and display stands.

Required Antigravity 2.0 actions:

1. Import source assets without renaming internal mesh nodes unless a deterministic mapping table is written.
2. Generate or assign colliders from `colliders.json` when available.
3. Create engine prefabs with a stable prefab ID equal to the source filename stem.
4. Add category-specific scripts/components according to the prefab schema.
5. Generate LOD/proxy variants for repeated world assets.
6. Validate prefab spawn, render, collision, and unload behavior.

## Pack 5: VFX Proxy Assets

Primary ZIP: `05_vfx_proxy_combat_telegraph_glb_pack_v1.zip`
Detected GLBs: `68`
Detected WAVs: `0`

Production focus: turn static VFX meshes into timed VFX prefabs with emissive/additive materials, scaling curves, despawn, and hitbox sync.

Required Antigravity 2.0 actions:

1. Import source assets without renaming internal mesh nodes unless a deterministic mapping table is written.
2. Generate or assign colliders from `colliders.json` when available.
3. Create engine prefabs with a stable prefab ID equal to the source filename stem.
4. Add category-specific scripts/components according to the prefab schema.
5. Generate LOD/proxy variants for repeated world assets.
6. Validate prefab spawn, render, collision, and unload behavior.

## Pack 6: Animation Placeholder Pack

Primary ZIP: `06_animation_placeholder_pose_glb_pack_v1.zip`
Detected GLBs: `12`
Detected WAVs: `0`

Production focus: use pose proxies as animation reference targets; create humanoid/quadruped placeholder state machines.

Required Antigravity 2.0 actions:

1. Import source assets without renaming internal mesh nodes unless a deterministic mapping table is written.
2. Generate or assign colliders from `colliders.json` when available.
3. Create engine prefabs with a stable prefab ID equal to the source filename stem.
4. Add category-specific scripts/components according to the prefab schema.
5. Generate LOD/proxy variants for repeated world assets.
6. Validate prefab spawn, render, collision, and unload behavior.

## Pack 7: UI/HUD 3D/2D Worldspace Markers

Primary ZIP: `07_ui_hud_worldspace_markers_glb_pack_v1.zip`
Detected GLBs: `12`
Detected WAVs: `0`

Production focus: convert markers to worldspace UI prefabs with billboard behavior, distance fading, occlusion rules, and accessibility scaling.

Required Antigravity 2.0 actions:

1. Import source assets without renaming internal mesh nodes unless a deterministic mapping table is written.
2. Generate or assign colliders from `colliders.json` when available.
3. Create engine prefabs with a stable prefab ID equal to the source filename stem.
4. Add category-specific scripts/components according to the prefab schema.
5. Generate LOD/proxy variants for repeated world assets.
6. Validate prefab spawn, render, collision, and unload behavior.

## Pack 8: Quest Interaction Objects

Primary ZIP: `08_quest_interaction_objects_glb_pack_v1.zip`
Detected GLBs: `15`
Detected WAVs: `0`

Production focus: add interactable components, quest state visibility, objective highlighting, and completion logic.

Required Antigravity 2.0 actions:

1. Import source assets without renaming internal mesh nodes unless a deterministic mapping table is written.
2. Generate or assign colliders from `colliders.json` when available.
3. Create engine prefabs with a stable prefab ID equal to the source filename stem.
4. Add category-specific scripts/components according to the prefab schema.
5. Generate LOD/proxy variants for repeated world assets.
6. Validate prefab spawn, render, collision, and unload behavior.

## Pack 9: Mounts, Vehicles, and Travel

Primary ZIP: `09_mounts_vehicles_travel_glb_pack_v1.zip`
Detected GLBs: `12`
Detected WAVs: `0`

Production focus: add ride/board/use components, nav constraints, docking points, camera anchors, and travel transitions.

Required Antigravity 2.0 actions:

1. Import source assets without renaming internal mesh nodes unless a deterministic mapping table is written.
2. Generate or assign colliders from `colliders.json` when available.
3. Create engine prefabs with a stable prefab ID equal to the source filename stem.
4. Add category-specific scripts/components according to the prefab schema.
5. Generate LOD/proxy variants for repeated world assets.
6. Validate prefab spawn, render, collision, and unload behavior.

## Pack 10: Enemy Faction Architecture

Primary ZIP: `10_enemy_faction_architecture_glb_pack_v1.zip`
Detected GLBs: `12`
Detected WAVs: `0`

Production focus: make faction structures, hazards, spawn anchors, turrets, nest objects, and destructible objectives into prefabs.

Required Antigravity 2.0 actions:

1. Import source assets without renaming internal mesh nodes unless a deterministic mapping table is written.
2. Generate or assign colliders from `colliders.json` when available.
3. Create engine prefabs with a stable prefab ID equal to the source filename stem.
4. Add category-specific scripts/components according to the prefab schema.
5. Generate LOD/proxy variants for repeated world assets.
6. Validate prefab spawn, render, collision, and unload behavior.

## Pack 11: Boss Arena Set Pack

Primary ZIP: `11_boss_arena_mechanics_glb_pack_v1.zip`
Detected GLBs: `12`
Detected WAVs: `0`

Production focus: build boss arena prefabs with mechanic anchors, climbing points, phase triggers, lane markers, hazard zones, and camera volumes.

Required Antigravity 2.0 actions:

1. Import source assets without renaming internal mesh nodes unless a deterministic mapping table is written.
2. Generate or assign colliders from `colliders.json` when available.
3. Create engine prefabs with a stable prefab ID equal to the source filename stem.
4. Add category-specific scripts/components according to the prefab schema.
5. Generate LOD/proxy variants for repeated world assets.
6. Validate prefab spawn, render, collision, and unload behavior.

## Pack 12: Audio Placeholder Pack

Primary ZIP: `12_audio_placeholder_cue_pack_v1.zip`
Detected GLBs: `0`
Detected WAVs: `24`

Production focus: import WAV cues as audio events, group by mixer, add spatialization, variation, cooldown, and replacement hooks.

Required Antigravity 2.0 actions:

1. Import source assets without renaming internal mesh nodes unless a deterministic mapping table is written.
2. Generate or assign colliders from `colliders.json` when available.
3. Create engine prefabs with a stable prefab ID equal to the source filename stem.
4. Add category-specific scripts/components according to the prefab schema.
5. Generate LOD/proxy variants for repeated world assets.
6. Validate prefab spawn, render, collision, and unload behavior.
